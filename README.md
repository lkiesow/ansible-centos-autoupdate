CentOS 8: Enable Auto-Update
============================

Ansible playbook for enabling automatic package updates on CentOS 8.
Adjust the `hosts.yml`, then run with:

```
ansible-playbook autoupdate.yml
```
